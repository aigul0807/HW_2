# -*- coding: utf-8 -*-
"""notes_3_1.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1tCBH1aHlmJPjW0CD5x-Qc6SEw2nzAZV4
"""

# Машинное обучение -- п/область ИИ, ср-во создания моделей данных

# ML решает след. задачу:
# Требуется подогнать заданный набор точек с данными под соответствующую функцию (ф-ия отображает вход на выход),
# которая улавливает важные сигналы в данных и игнорирует помехи, а затем убеиться, что на новых данных
# функция работает хорошо

"""Задача №1. Человек живет в шаговой доступности от магазина "Пятерочка", в котором обычно покупает продукты. У магазина есть статичтсика по его посещению. В первую неделю человек посетил магазин 5 раз, во вторую -- 6, в третью -- 4, в четвертую -- 5. Сколько раз посетит человек магазин в пятую неделю при таких исходных данных? (4-6, 5)

ОТВЕТ: 5, потому что человек по своей природе каждый день хочет есть приблизительно одинаково, поэтому можно предп., что его походы в магазин -- это случайные величины, которые скорее всего будут соответсвовать равновероятному распределению. ТАКОВЫ УСЛОВИЯ ЗАДАЧИ.

Задача №2. Есть два дома, в каждом из которых есть своя футбольная дворовая команда. Эти команды каждую неделю проводят футб. матчи. По ним собралась следующая статистика: 1 неделя -- счет 3:2, 2 неделя -- 3:3б 3 неделя -- 1:3, 4 неделя -- 2:3. В рамках этих встреч соот-но было забило мячей: 5, 6, 4, 5. Сколько мячей м.б. забито в матче, который произойдет на 5ой неделе? (4-6, 5)

ОТВЕТ: 4-6. ответ 5 не верен, т.к. сумма голов не равновероятна, распределение Пуассона. Условия другие, хотя значение одинаковы.

Как в целом раюотает машинное обучение?
___
___1 ЭТАП___

1) Дан набор данных

2) Данные поступают нек процесса-обучения (пока что пр-сс обучения не важен)

3) Рез-т №1-- нек. модель №1 для решения конкр. задачи

4) Как проверить модель? Предказание-процедура (w). На ее вход пост. модель, кот. мы придумали. На выходе -- нек. результат. Если рез. устраивает нас -- модель отличная! + Надо убдеиться, что на новых данных она работает хорошо.

(В предыдущих примерах модель -- срзнач)
___
___2 ЭТАП___

1) Берем модель №1

2) к нам откуда-то приходят новые данные

3) мы запускаем процедуру предсказания (w)

4) получаем рез-т №2

!!!Большое значение имеет источник данных, его надо пытаться оценить исходя из условий задачи
___
Проблемы ML:

1) Источник данных

2) полученный после применения рез-т, его нельзя объяснить. как доказать?

- Обучение с учителем (supervised learning)

моделирует отношение-связи м/у признаками и метками. такие модели служат для предсказания меток на основе обучающих данных маркированных. после построения модели можно использовать ее для присвоения новых меток новым раннее неизвестным данным.

- - задачи классификации (метки дискретные: два или более, их понятное кол-во а не беск-ть)
- - задачи регрессии (метки/рез-т, непрерывные величины)
___
- Обучение без учителя (unsupervised learning)

моделирование признаков без меток. такие модели служат для выявления структуры немаркированных данных.

- - задачи кластеризации (выделяет отдельные группы данных среди всех данных по признаку)
- - понижения размерности (поиск более сжатого представления данных, из данных убирается избыточность)
___
- Частичное обучение (semi-supervised learning)

Метки неполные, не все данные пронумерованы
___
- Методы обучения с подкреплением (reinforcement learning)

такие системы обучения улучшают свои хар-ки на основе взаимодействия какой-то обратно связи со средой, в кот. она работает. при этом взаимодействии  ситема получает сигналы (функции наград), которые несут себе инф-у, насколько хорошо/плохо сисема решиал задачу с точки зрения среды, в кот. она работает. итоговая награда не станет максимальной.
"""

import seaborn as sns

iris = sns.load_dataset('iris')

print(iris.head())
print(type(iris))

print(type(iris.values))
print(iris.values.shape)
print(iris.columns)
print(iris.index)

""" - Строки датасета -- отдельные объекты -- образцы (sample)
 - Столбцы -- признаки (feature)-- сответствуют конкретным наблюдениям
 - матрица признаков (feature matrix) размер[число образцов х число признаков]
 - целевой массив, массив меток (targets) -- обычно это одномерный массив[1 х число образцов] -- данные, кот-е мы хотим предсказать на сонове имеющихся данных
 ___
 - зависимые переменные (метка)
 - НЗ переменные (признаки
 )
 ___
 Пр-сс построения системы ML:

 1. Предварительная обработка
 - на вход поступают необработанные данные и метки
 - присходит выбор признаков, масштабирование признаков
 - понижение размерности
 - выборка образцов
 - на выход поступает набор данных: обучающий и тестовый
 2. Обучение модели
 - выбор модели
 - перекретсная проверка (методики, при кот-х данные каким-то образом делятся, чтобы больше инф-ы от них получить)
 - метрики эффективности
 - оптимизация гиперпараметров (их получаем не из данных, а они явл. хар-ками настраиваемыми у модели)
 3. оценка и формирование финальной модели
 4. прогнозирование (использование модели)

___
SciKit-learn

1. Выбираем класс модели
2. устанавливаем гиперпараметры модели
3. создаем матрицу признаков и целевой массив
4. обучение модели fit()
5. применять модель к новым данным
- predict() -- с учителем
- predict() или transform() -- без учителя
"""

# ОБУЧЕНИЕ С УЧИТЕЛЕМ: линейная регрессия
## простая лин. регрессия

# пусть есть набор точек, хотим их аппроксимировать нек. прямой
# y = ax + b

import matplotlib.pyplot as plt
import numpy as np

# I.

np.random.seed(1)
x = 10*np.random.rand(50)

y = 2 * x - 1 + np.random.randn(50)

plt.scatter(x, y)

# II.
# 1. выбираем класс модели
from sklearn.linear_model import LinearRegression

# 2. устанавливаем гиперпараметры модели
model = LinearRegression(fit_intercept=True)

# 3. создаем матрицу признаков и целевой массив

print(x.shape)
print(y.shape)

X = x[:, np.newaxis]
# 4. обучение модели fit()
model.fit(X, y)

print(model.coef_[0])
print(model.intercept_)

x_ = np.linspace(0, 10, 30)
y_ = model.coef_[0] * x_ + model.intercept_

plt.plot(x_, y_)
# 5. применение модели к новым данным

xfit = np.linspace(-10, 10, 5)
yfit = model.predict(xfit[:, np.newaxis])

plt.scatter(xfit, yfit)
plt.show()